# compareMS2 binaries

This folder contains only the binary executables that perform the computations for the compareMS2 GUI.
The source code for these are stored in a [separate repository](https://github.com/524D/compareMS2-cmd), because they can be useful
on their own, without the full GUI.
